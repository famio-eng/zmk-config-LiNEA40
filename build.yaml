# This file generates the GitHub Actions matrix.
# For simple board + shield combinations, add them to the top level board and
# shield arrays, for more control, add individual board + shield combinations
# to the `include` property.
# include:
#   - board: nice_nano_v2
#     shield: corne_left
#     cmake-args: -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=y
#     artifact-name: corne_central

# LiNEA40の左右ビルドに必要な設定を定義
include:
  - board: seeeduino_xiao_ble
    shield: LiNEA40_left rgbled_adapter
    # 左右両方に Studio 設定を適用（設定はoverlayファイルに任せる）
    cmake-args: -DCONFIG_ZMK_STUDIO=y
  
  - board: seeeduino_xiao_ble
    shield: LiNEA40_right rgbled_adapter
    # 🚨 snippet: studio-rpc-usb-uart の行を完全に削除
    cmake-args: -DCONFIG_ZMK_STUDIO=y

  # 設定リセットビルドを残したい場合は含める
  # - board: seeeduino_xiao_ble
  #   shield: settings_reset
