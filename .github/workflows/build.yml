on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.2.1
    env:
      # 環境変数ZMK_STUDIO_SNIPPETを空にすることで、デフォルトで参照されるスニペットを無効化する
      ZMK_STUDIO_SNIPPET: ''
