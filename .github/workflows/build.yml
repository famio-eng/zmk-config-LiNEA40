on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # 🚨 スニペットを無効にする環境変数をジョブ全体に設定
    env:
      ZMK_STUDIO_SNIPPET: ''
    
    # 🚨 再利用可能なワークフローをトップレベルで呼び出す（元の正しい構造）
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.2.1
