on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.2.1
    with:
      # ZMK Studioスニペットの参照を明示的に空に設定し、DTSエラーを回避する
      # config/studio.overlay は引き続き有効です。
      SNIPPET: ''
