on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # 🚨 DTSエラー回避のための環境変数
    # (ZMK Actionに渡され、スニペットの参照を無効化します)
    env:
      ZMK_STUDIO_SNIPPET: ''
    
    # 🚨 再利用可能なワークフローの呼び出し（元の正しい構造）
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.2.1
